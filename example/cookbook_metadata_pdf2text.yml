name: pdf2text_metadata
description: recipe to add dummy exmaple metadata
class: _metadata
extensions:
  - pdf
recipes:
  pdf2text:
    metadata:
      - file: "pdf.txt"
        fulltext: true
    execs:
      - service: metadata
        extension: "<nil>"
        commands:
          - prog: java
            stdout:
              type: file
              file: "pdf.txt"
            args:
              - -jar
              - server/recipe/tika-app-1.26.jar
              - --text
              - type: file_in
                file:
                  name: "source.file"
                  url: "%_source.url%"
