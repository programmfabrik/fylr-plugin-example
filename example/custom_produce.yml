custommetadata:
  - cookbook: fylr_example:pdf2text_metadata
    recipe: pdf2text
    extensions:
      - pdf
    include_versions: true

classes:
  office:
    uploadenabledextensions:
      - pdf
    custommetadata:
      - cookbook: fylr_example:pdf2text_metadata
        recipe: pdf2text

    # all office documents will run this metadata item
    # versions:
    #   # for originals in class office produce additional metadata
    #   - name: original
    #     extensions:
    #       - pdf
    #     custommetadata:
    #       - cookbook: fylr_example:example_metadata
    #         recipe: example
  image:
    custommetadata:
      - cookbook: fylr_example:example_metadata
        recipe: example
    versions:
      - name: example
        extensions:
          - jpg
          - png
        rightsmanagement: true
        cookbook: fylr_example:example_image
        recipe: grayIM
        zoomable: true
